# Task rows — description dot instead of preview text

No description text in task lists. A small dot right of the title: blue = has a
description (read), amber = description updated since last opened (builder's pick for
the "new" color). Opening the edit modal marks seen (localStorage per device). Pairs
with the personal agent's annotations.
